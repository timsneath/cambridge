name: spectrum
description: Project Cambridge
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  collection: ^1.15.0-nullsafety.5
  flutter:
    sdk: flutter
  google_fonts:
    git:
      url: git@github.com:material-foundation/google-fonts-flutter.git
      ref: nnbd
  typed_data: ^1.3.0-nullsafety.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.1

flutter:
  uses-material-design: true
  assets:
    - assets/google.scr
    - roms/48.rom
    # - roms/TIME.SNA
    # - roms/JETSET.Z80